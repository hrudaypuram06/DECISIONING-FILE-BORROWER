{
  "name": "credifair-root",
  "private": true,
  "version": "1.2.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend -- --port 3000",
    "build": "cd frontend && npm install && npm run build",
    "start": "npm run dev --prefix frontend -- --port 3000"
  }
}
